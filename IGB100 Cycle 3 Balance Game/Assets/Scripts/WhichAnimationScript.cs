using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WhichAnimationScript : MonoBehaviour
{
    
    [Header("If the bool below is true,")]
    [Header("then use animation 1, ")]
    [Header("if false then use animation 2")]
     
    [Space(23)]
    public bool whichAnimation;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
